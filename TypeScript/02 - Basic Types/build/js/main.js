"use strict";
const myName = 'Mateus';
let myLastName;
let myAge; // it can hold a string or a number
let isLoading;
let album; // any type of data, string, number, boolean. (avoid this)
const sum = (a, b) => {
    return a + b;
};
